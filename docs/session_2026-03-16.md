# Session 2026-03-16

## Summary

Completed security hardening, performance optimization, centralized logging implementation, and fixed Grafana dashboards.

## Major Accomplishments

### 1. Security Hardening (All Issues Resolved)

| Issue | Resolution |
|-------|------------|
| UFW not enabled on re-node-02 | Enabled and configured |
| Monitoring exporters exposed | Bound to Tailscale IPs only |
| SSH open to internet | Fail2Ban configured on all servers |
| HAProxy stats exposed | Restricted to Tailscale |
| Grafana exposed | Restricted to Tailscale |
| Missing security headers | Added to HAProxy HTTPS frontend |
| PHP disable_functions empty | Configured with dangerous functions blocked |
| Nginx server_tokens | Disabled |

**Security Score:** 7.5/10 → 8.5/10

### 2. Performance Optimization

| Component | Changes |
|-----------|---------|
| **PostgreSQL** | max_connections=200, work_mem=128MB, huge_pages=on |
| **PHP-FPM** | Production: max_children=40, staging: max_children=15, slowlog enabled |
| **HAProxy** | maxconn=65535, nbthread=2, optimized timeouts |
| **Redis** | tcp-backlog=4096, hz=100, slowlog=1ms |
| **Kernel** | VPS-tuned sysctl for all servers, router/database-specific tuning |
| **System Limits** | file-max=65535, systemd service overrides |

### 3. Backup & Recovery

- Daily PostgreSQL backups to Cloudflare R2
- Daily Redis backups to Cloudflare R2
- 30-day retention
- Automated cleanup of old backups
- Restore procedures documented

**R2 Bucket:** `quantyra-backup`

### 4. Centralized Logging (Loki)

- Loki installed on router-01
- Promtail installed on all 7 servers
- Log collection: syslog, auth, nginx, php-fpm, postgresql, redis, haproxy, patroni
- 31-day retention
- Grafana datasource configured

### 5. Monitoring & Alerting

- 7 alert groups configured (node, postgresql, redis, haproxy, phpfpm, nginx, etcd)
- Slack and email notifications
- Alert thresholds documented

### 6. Grafana Dashboard Fixes

| Dashboard | Fixes |
|-----------|-------|
| Node Exporter | Fixed disk duplication, aggregated by `node` |
| PHP-FPM | Changed to rate metrics, aggregated by `node` |
| Redis | Aggregated all metrics by `node` |
| PostgreSQL/HAProxy | Fixed Patroni queries, HAProxy frontend names |
| Nginx | Aggregated by `node`, fixed exporter stub_status endpoint |

## Configuration Files Updated

### Prometheus
- `/etc/prometheus/prometheus.yml` - Added `node` and `role` labels
- `/etc/prometheus/rules/alerts.yml` - 7 alert groups

### Loki/Promtail
- `/etc/loki/loki-config.yaml`
- `/etc/promtail/promtail-config.yaml` (per server)

### HAProxy
- `/etc/haproxy/haproxy.cfg` - Performance tuning
- `/etc/haproxy/domains/*.cfg` - Web frontends
- `/etc/systemd/system/haproxy.service.d/override.conf`

### PHP-FPM
- `/etc/php/8.5/fpm/pool.d/rentalfixer.conf`
- `/etc/php/8.5/fpm/pool.d/rentalfixer-staging.conf`

### Redis
- `/etc/redis/redis.conf` (re-node-01, re-node-03)

### System
- `/etc/sysctl.d/99-vps-tuning.conf`
- `/etc/sysctl.d/99-router-tuning.conf`
- `/etc/sysctl.d/99-database-tuning.conf`
- `/etc/security/limits.d/99-infra.conf`

### Backup
- `/usr/local/bin/backup-postgres.sh`
- `/usr/local/bin/backup-redis.sh`
- `/etc/rclone/rclone.conf`

## Cleanup Performed

- Removed old backup scripts from all servers
- Removed old backup services (nightly-backup, pg-backup, etc.)
- Stopped and disabled Caddy on re-db
- Removed old rclone configs

## Files Synced to Repo

```
configs/
├── backup/           # Backup scripts and rclone config
├── grafana/         # Dashboards and provisioning
├── haproxy/         # router-01 and router-02 configs
├── loki/            # Loki configuration
├── nginx/           # re-db and re-node-02 configs
├── php-fpm/         # Pool configs and php.ini
├── prometheus/      # Prometheus and Alertmanager configs
├── promtail/        # Per-server Promtail configs
├── redis/           # re-node-01 and re-node-03 configs
├── sysctl/          # Kernel tuning configs
├── system/          # limits.conf
└── systemd/         # All systemd service files and overrides
```

## Services Running

### router-01
- haproxy
- prometheus
- alertmanager
- grafana-server
- loki
- promtail
- etcd
- node_exporter
- haproxy_exporter
- dashboard

### router-02
- haproxy
- etcd
- promtail
- node_exporter
- haproxy_exporter

### re-db
- nginx
- php8.5-fpm
- promtail
- node_exporter
- prometheus-nginx-exporter
- php-fpm-exporter

### re-node-02
- nginx
- php8.5-fpm
- promtail
- node_exporter
- prometheus-nginx-exporter
- php-fpm-exporter

### re-node-01
- patroni
- redis-server
- promtail
- node_exporter
- postgres_exporter
- redis_exporter

### re-node-03
- patroni
- redis-server
- promtail
- node_exporter
- postgres_exporter
- redis_exporter

### re-node-04
- patroni
- etcd
- promtail
- node_exporter
- postgres_exporter

## Remaining Tasks

- [ ] Implement secrets management (Vault/SOPS)
- [ ] Security audit logging
- [ ] Penetration testing
- [ ] Disaster recovery testing
- [ ] Incident response procedures

## Notes

- PostgreSQL cluster was reinitialized on 2026-03-16 to apply max_connections change
- All monitoring exporters now bind to Tailscale IPs only
- HAProxy exporter fixed to use stats endpoint with authentication
- All Grafana dashboards now use `node` labels for cleaner display